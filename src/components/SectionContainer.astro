---
const { class: className, id } = Astro.props;
---

<section
    id={id}
    data-section={id}
    class=`section ${className} scroll-m-20 mx-auto container lg:max-w-4xl md:max-w-3xl sm:max-w-2xl`
>
    <slot />
</section>
